namespace Feliz

/// Describes a basic react element
type IReactAttribute = interface end

/// Describes a basic style attribute
type IStyleAttribute = interface end

type IBorderStyle = interface end

type ITextAlignment = interface end

type ITextDecorationStyle = interface end

type ITextDecorationLine = interface end

type IVisibilityStyle = interface end

type IPositionStyle = interface end

type IAlignContentStyle = interface end

type IAlignItemStyle = interface end

type IAlignSelfStyle = interface end

type IDisplayStyle = interface end